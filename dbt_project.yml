name: 'maven_analytics'
version: '1.0.0'
config-version: 2

profile: 'maven_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

models:
  maven_analytics:
    +schema: maven_data
    +materialized: view